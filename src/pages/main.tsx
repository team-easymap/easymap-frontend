import MainSearchComponent from '@/components/main/search';

const MainPage = () => {
  return (
    <main className='w-full'>
      <MainSearchComponent />
    </main>
  );
};

export default MainPage;
